# Local development can use the proxy in package.json, so this can stay blank.
# For Vercel, set this to your Render backend URL, for example:
# REACT_APP_API_BASE_URL=https://graph-analyse-api.onrender.com
REACT_APP_API_BASE_URL=

# Optional Create React App settings.
GENERATE_SOURCEMAP=false
